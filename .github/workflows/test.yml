on: [push, workflow_dispatch, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: fkirc/skip-duplicate-actions@master
        id: skip
        with:
          github_token: ${{ github.token }}
      - if: ${{ steps.skip.outputs.should_skip == 'false' }}
        run: |
          echo "Running slow tests..." && sleep 60
          echo "Do other stuff..."

  private_test:
    runs-on: ubuntu-latest
    steps:
      # To use this repository's private action, you must check out the repository
      - run: echo "Pre-test waiting..." && sleep 20
      - name: Checkout
        uses: actions/checkout@v2
      - name: skip-duplicate-actions private checkout
        uses: ./ # Uses this action in the root directory
        id: skip
        with:
          github_token: ${{ github.token }}
      - if: ${{ steps.skip.outputs.should_skip == 'false' }}
        run: |
          echo "Running slow stuff..." && sleep 40
